prelude: |
  $LOAD_PATH.unshift File.expand_path('lib')
  require 'string_bits'
  srand(42)
  DATA = Random.bytes(1_000_000)

benchmark:
  baseline: |
    result = DATA.dup
    DATA.bytesize.times { |i| result.setbyte(i, ~DATA.getbyte(i) & 0xFF) }
    result
  string_bits: |
    DATA.bitwise_not
